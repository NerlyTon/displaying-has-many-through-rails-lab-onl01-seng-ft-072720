<h2><%= @patient.name%></h2>
<p>
  Age: <%= @patient.age %>
</p>
Appointments:<br>
  <% @patient.appointments.each do |appt| %>
    <%= link_to appt.doctor.name, doctor_path(appt.doctor) %> Date:
    <%= appt.appointment_datetime.strftime("%B %e, %Y at %H:%M") %> <br>
  <% end %>